<!-- loom:setup-hermes:begin -->
# {{PROJECT_NAME}} — project context

> This section is generated by loom's Hermes setup. It holds **project conventions**, not workflow
> steering (that lives in each profile's `SOUL.md`, per [ADR-002](https://github.com/zentetsukenz/agent)).
> loom edits only this marked section; your own content outside it is untouched. Hermes reads this
> file natively (priority: `.hermes.md`/`HERMES.md` → `AGENTS.md` → `CLAUDE.md`).

## Build & test

{{PROJECT_CONTEXT}}

## Quality baseline

{{QUALITY_BASELINE}}

## Communication protocol (handoffs)

This project uses loom's Seam Artifact Protocol. The communication protocol document is at
`{{PROTOCOL_POINTER}}` (committed; always-on because Hermes has no description-triggered
instruction). Read it before producing or discovering a handoff.
<!-- loom:setup-hermes:end -->
