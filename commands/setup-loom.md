---
description: Set up loom's SDLC framework inside the Mirai VS Code harness for this project — interview-driven, generates .mirai/agents, .mirai/prompts, .mirai/skills, and AGENTS.md.
---

Set up loom for Mirai: $ARGUMENTS

Use the `setup-loom` skill in `init` mode. Explore the target project first, then interview using `grill-with-docs` per `SKILLS/meta/setup-loom/references/interview.md`, present the proposed `.mirai/` tree, confirm, write in Mirai's exact format, verify per `references/verify.md`, and report created paths.
</content>
